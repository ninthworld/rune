name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Least privilege, declared rather than inherited: an undeclared token takes the repository
# default, which can be widened in Settings without touching this file. These jobs build and
# test a checkout and talk to nobody, so read is all they get, and no job below raises it.
permissions:
  contents: read

# Every `uses:` below is pinned to a full-length commit SHA: a tag is mutable, and whoever
# can move `v4` can change what runs in CI — which is where the credentials are. The trailing
# comment is the release that SHA belongs to, so the pin stays reviewable and Dependabot can
# bump it. Each pin is the newest release *within the major this workflow already used*, so
# this is a hardening change and not a silent upgrade.
jobs:
  engine:
    name: Engine
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      # Pinned by SHA, `dtolnay/rust-toolchain` can no longer read the toolchain off the ref
      # — `@stable` *was* the input — so it must be named explicitly. Without this the
      # install fails rather than quietly defaulting to stable.
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9 # v1
        with:
          toolchain: stable
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      # Run through the Make targets so CI and `make check` use identical commands;
      # the Makefile is the single source of truth for fmt/clippy/test options.
      - name: Lint (fmt + clippy)
        run: make engine-lint
      - name: Test
        run: make engine-test

  client:
    name: Client
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: clients/web/package-lock.json
      # `make client-audit client-check` runs `npm ci` once (shared prerequisite),
      # then audit, lint, typecheck, test, and build — identical to the local gate.
      - name: Check (audit + lint + typecheck + test + build)
        run: make client-audit client-check

  # Browser smoke canary (ADR 0011, issue #279): the one Playwright spec that drives a
  # real Chromium against a real seeded rune-server and plays real turns through the
  # rendered UI — most importantly the StrictMode canvas-attach guard (#276). Kept a
  # separate job (not folded into Engine/Client) because it needs a browser, a dev
  # server, and the server binary; `make check` stays fast and browser-free.
  smoke:
    name: Smoke
    runs-on: ubuntu-latest
    # Only skip the auto browser download during `npm ci`; the explicit
    # `playwright install` below still fetches the browser this job actually needs.
    env:
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: 1
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: clients/web/package-lock.json
      - uses: dtolnay/rust-toolchain@e97e2d8cc328f1b50210efc529dca0028893a2d9 # v1
        with:
          toolchain: stable
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
      # Install the client deps first so the Playwright CLI exists, then fetch just the
      # Chromium this job needs (plus its OS libs). No pre-installed browser here, so
      # `RUNE_PW_EXECUTABLE` stays empty and the config uses Playwright's managed one.
      - name: Install client deps
        run: cd clients/web && npm ci
      - name: Install Playwright Chromium
        run: cd clients/web && npx playwright install --with-deps chromium
      # `make smoke` builds the server binary and runs the spec — the exact target
      # `make verify` runs locally, so CI and the local gate stay in lockstep.
      - name: Smoke (browser canary)
        run: make smoke
