{
  "name": "@rune/web-e2e",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Browser smoke suite for the RUNE web client (ADR 0011). A separate package on purpose: the Playwright toolchain never lands in the `make check` client install.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.56.0",
    "@types/node": "22.5.0",
    "typescript": "5.5.4"
  }
}
