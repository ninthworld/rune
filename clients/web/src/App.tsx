/**
 * Placeholder shell. The client renders a `GameView` and returns an `action_id`; none of that
 * exists yet. This file is here so the build pipeline is real and exercised — it is replaced
 * wholesale by the first playable surface, not extended.
 */
export function App() {
  return (
    <main>
      <h1>SAGE</h1>
      <p>No client yet. The protocol mirror is in place; the playable surface is next.</p>
    </main>
  )
}
