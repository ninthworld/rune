/**
 * RUNE protocol — stable barrel re-export of protocol module.
 *
 * This file maintains backward compatibility by re-exporting all protocol types
 * and functions from the organized protocol/ module structure. See
 * `src/protocol/index.ts` for the complete protocol contract organization.
 */

export * from './protocol/index.js';
