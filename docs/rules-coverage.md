# Rules coverage

The authoritative record of which *Magic: The Gathering* Comprehensive Rules
(CR) the RUNE engine actually implements today. Every row points at real engine
code and a real test that exercises it.

## Scope

This document lists **implemented** CR rules only. It is a coverage map, **not a
TODO list** of unimplemented rules — a rule that RUNE does not model yet simply
has no row here. When a rule is only *partially* modeled, its row is marked
`partial` and the gap is named explicitly, but the deferred remainder is not
enumerated as future work. For the shape of the engine and where these pieces
live, see [`brief.md`](brief.md) and the ADRs under [`decisions/`](decisions/).

Rule numbers follow the CR citation convention in
[`coding-standards.md`](coding-standards.md): engine behavior that implements a
CR rule cites `CR NNN.Nx` in its doc comment, and any PR that adds or changes
rule behavior updates this table in the same PR. The table is seeded by auditing
those citations:

```
rg 'CR \d+' crates/rune-engine/src
```

Code and test anchors are given as `path :: item`. Paths are relative to the
repository root; all code is in `crates/rune-engine/src/`.

## Coverage

| CR rule | Summary | Status | Code anchor | Test anchor |
| --- | --- | --- | --- | --- |
| CR 103.5 | The London mulligan: after opening hands, each player may keep or mulligan; a mulligan shuffles the hand into the library and redraws a full opening hand, and a player who keeps after N mulligans puts N cards on the bottom of their library (a single multi-select requirement slot). Turn 1 begins only once every player has kept. | partial — turn-order/APNAP nuance is modeled as a simpler seat-by-seat sequential decision (the simultaneous-decision UI treatment is out of scope, issue #111); a first-hand keep works over the wire, while projecting the bottoming requirement rides the same engine→wire `requirements` wiring pending for targeting (ADR 0009 follow-up #73). | `mulligan.rs :: MulliganState`, `apply.rs :: apply_mulligan`, `apply.rs :: apply_keep`, `mulligan.rs :: bottom_requirement` | `mulligan.rs :: cr_103_5_nth_mulligan_requires_bottoming_n_cards`, `mulligan.rs :: cr_103_5_turn_one_waits_until_all_players_keep` |
| CR 104.2a | A player still in a game that all that player's opponents have left wins that game. In a two-player game, when one player has lost the other wins; the terminal result is derived on demand and surfaced on the wire (`GameView.result`). | implemented | `state.rs :: GameState::result`, `state.rs :: GameState::is_over`, `view.rs :: result_view` | `state.rs :: cr_104_2a_last_player_standing_wins`, `apply.rs :: issue_119_terminal_state_rejects_further_actions_purely_cr_104_2a`, `view.rs :: issue_119_terminal_result_projects_onto_the_view` |
| CR 104.3a | A player can concede the game at any time; that player leaves the game and loses. Concede is always offered in `valid_actions` regardless of phase. | implemented | `actions.rs :: valid_actions` (`offer_concede`), `apply.rs :: apply_concede` | `actions.rs :: issue_119_concede_is_always_offered_cr_104_3a`, `apply.rs :: issue_119_concede_ends_the_game_with_the_opponent_as_winner_cr_104_3a` |
| CR 104.4a | A game is a draw if all remaining players lose simultaneously; there is no winner. | implemented | `state.rs :: GameState::result` | `state.rs :: cr_104_4a_simultaneous_loss_is_a_draw` |
| CR 115 / 115.1 / 115.4 | A spell or ability targets objects/players as defined by a target spec; legality is derived on demand from current state. `AnyTarget` is the modern "any target" (a creature or a player). | partial — only `AnyPlayer`, `AnyPermanent`, `AnyCreature`, `SpellOnStack`, `AnyTarget` specs exist; printed types are authoritative (no type-changing layer); planeswalkers/battles are not modeled, so `AnyTarget` is creatures + players. | `ability.rs :: TargetSpec`, `resolve.rs :: target_is_legal` | `resolve.rs :: target_legality_tracks_current_state`, `resolve.rs :: issue_148_spell_on_stack_target_is_legal_only_while_the_spell_is_on_the_stack`, `resolve.rs :: issue_149_any_target_is_legal_for_creatures_and_in_game_players` |
| CR 119 | Life gain and life loss adjust a player's life total; a life-loss effect can drop a player to 0 or less and feed the zero-life loss (CR 704.5a). | partial — only a fixed-amount gain/loss for the controller (`PlayerRef::Controller`) is modeled; other player references and life-total replacement effects are deferred. | `ability.rs :: Effect::GainLife`, `ability.rs :: Effect::LoseLife`, `apply.rs :: apply_effect` | `apply.rs :: issue_149_life_gain_adds_to_a_low_life_total_cr_119`, `apply.rs :: issue_149_life_loss_to_exactly_zero_triggers_the_loss_cr_704_5a` |
| CR 120 / 120.3a / 120.3d | Dealing damage from an effect: damage to a player is life loss; damage to a creature is marked on it (for the lethal-damage SBA, CR 704.5g). | partial — no prevention/replacement, no deathtouch/lifelink, no planeswalker or battle damage. | `ability.rs :: Effect::DealDamage`, `apply.rs :: apply_targeted_effect` | `apply.rs :: issue_149_burn_spell_kills_a_creature_via_lethal_damage_sba_cr_704_5g`, `apply.rs :: issue_149_burn_spell_to_a_player_drops_life_and_loses_at_zero_cr_704_5a` |
| CR 116.2a | Playing a land is a special action that does not use the stack. | implemented | `apply.rs :: apply_play_land` | `apply.rs :: issue_card_effects_etb_draw_end_to_end` |
| CR 117.1a | A player who has priority may cast an instant spell any time; a noninstant spell (sorcery, artifact, enchantment, creature) only during their main phase, with an empty stack (sorcery speed). `valid_actions` offers every castable hand card payable from the pool, exempting instants from the sorcery-speed gate. | partial — Auras are excluded pending spell targets-at-cast (issue #148); flash and other timing-altering keywords are not modeled. | `actions.rs :: valid_actions`, `actions.rs :: is_castable_spell`, `apply.rs :: apply_cast_spell` | `actions.rs :: issue_147_instant_castable_with_a_nonempty_stack_and_off_turn_cr_117_1a`, `actions.rs :: issue_147_sorcery_not_offered_off_turn_or_mid_stack_cr_304_1`, `actions.rs :: issue_147_unpayable_spells_are_never_offered`, `actions.rs :: issue_147_aura_is_uncastable_until_targets_at_cast_issue_148` |
| CR 304.1 | A player may cast a sorcery only during a main phase of their own turn, with an empty stack and priority (sorcery speed). | implemented | `actions.rs :: valid_actions` (sorcery-speed gate) | `actions.rs :: issue_147_sorcery_not_offered_off_turn_or_mid_stack_cr_304_1` |
| CR 307.1 | An enchantment (and, likewise, an artifact) is a permanent spell cast at sorcery speed; on resolution it enters the battlefield. | partial — Auras are gated off until targets-at-cast (issue #148); other enchantment/artifact subtypes carry no extra rules yet. | `actions.rs :: valid_actions`, `card.rs :: CardData::is_permanent`, `resolve.rs :: resolve_stack_object` | `actions.rs :: issue_147_artifact_and_enchantment_cast_at_sorcery_speed_and_enter_battlefield` |
| CR 608.1 | The stack resolves last-in, first-out: the top (most recently added) object resolves first. | implemented | `apply.rs :: apply_pass_priority`, `resolve.rs :: resolve_stack_object` | `actions.rs :: issue_147_cast_instant_resolves_after_a_later_instant_lifo_cr_608_1` |
| CR 122 | Counters on a permanent (`+1/+1`, `-1/-1`) are stored per kind and counted on demand, and are placed by the `PutCounters` effect (which folds into computed P/T, CR 613.7c — a `-1/-1` counter can turn lethal). | partial — only the two P/T counter kinds are modeled; loyalty/charge/etc. deferred. | `state.rs :: CounterKind`, `state.rs :: Permanent::counter_count`, `ability.rs :: Effect::PutCounters`, `apply.rs :: apply_targeted_effect` | `characteristics.rs :: counter_count_defaults_to_zero_and_reports_stored_counts`, `resolve.rs :: issue_149_put_counters_ability_lands_on_its_target_cr_122`, `apply.rs :: issue_149_minus_one_counter_lowers_toughness_to_lethal_cr_704_5g` |
| CR 205.4 | A card's supertypes are a closed, structured set (never a parsed string). | implemented | `card_type.rs :: Supertype` | `card.rs :: type_line_renders_supertypes_types_and_subtypes` |
| CR 300 | A card's card types are a closed, structured set. | implemented | `card_type.rs :: CardType` | `card.rs :: has_type_and_has_subtype_query_structured_types` |
| CR 302.6 | Summoning sickness: a creature can't attack (or use `{T}`/`{Q}` abilities) unless its controller has controlled it continuously since their most recent turn began. | partial — the attack restriction is enforced (with the haste exemption, CR 702.10b, applied at the `attacker_candidates` call site); the tap/untap-ability restriction is not modeled yet. | `combat.rs :: has_summoning_sickness`, `combat.rs :: attacker_candidates` | `combat.rs :: summoning_sickness_is_by_entry_turn_cr_302_6`, `apply.rs :: issue_117_summoning_sick_creature_cannot_attack_cr_302_6` |
| CR 508.1 / 508.1a / 508.1f | Declare attackers: the active player declares as attackers untapped creatures controlled since the turn began; declaring none is legal; attacking taps the attacker (unless it has vigilance, CR 702.20b). | partial — declaration only; single attack target (the opponent), no defender. Combat damage lands separately (CR 510, below). | `combat.rs :: attacker_candidates`, `actions.rs :: attackers_selection_is_legal`, `apply.rs :: apply_declare_attackers` | `apply.rs :: issue_117_declare_attackers_taps_and_marks_attackers_cr_508_1`, `apply.rs :: issue_117_empty_attacker_declaration_is_legal_cr_508_1a`, `apply.rs :: issue_117_tapped_creature_cannot_attack_cr_508_1a` |
| CR 509.1 / 509.1a | Declare blockers: the defending player assigns each blocker to one attacking creature; multiple blockers per attacker are allowed; a tapped creature can't block; a blocker must be able to block the attacker it is assigned to (flying evasion, CR 702.9c/702.17b). | partial — declaration only (blocking restrictions cover untapped + flying/reach evasion; no player-chosen damage-assignment order). Combat damage lands separately (CR 510, below). | `combat.rs :: blocker_candidates`, `combat.rs :: declared_attackers`, `combat.rs :: blocker_can_block_attacker`, `actions.rs :: blocks_selection_is_legal`, `apply.rs :: apply_declare_blockers` | `apply.rs :: issue_117_defender_declares_blockers_multiple_per_attacker_cr_509_1a`, `apply.rs :: issue_117_tapped_creature_cannot_block_cr_509_1a`, `apply.rs :: issue_117_a_creature_cannot_be_declared_as_two_blocks_cr_509_1a` |
| CR 510.1 / 510.1c / 510.2 | Combat damage: as a turn-based action, each attacking/blocking creature assigns combat damage equal to its power — unblocked attackers to the defending player, blocked attackers to their blockers, blockers to the attacker — dealt simultaneously and marked on creatures / subtracted from life. | partial — normal combat only, two players. No first strike / double strike (single damage step), no trample or deathtouch, and no player-chosen damage-assignment order: a blocked attacker's damage is split across its blockers in a deterministic default (battlefield order, just-lethal per blocker, remainder undealt). | `combat.rs :: combat_damage`, `apply.rs :: deal_combat_damage` | `apply.rs :: issue_118_unblocked_attacker_damages_the_defending_player_cr_510_1c`, `apply.rs :: issue_118_blocked_attacker_and_blocker_deal_lethal_and_both_die_cr_510_704_5g`, `apply.rs :: issue_118_multi_block_mutual_destruction_cr_510_1c`, `apply.rs :: issue_118_multi_block_assigns_lethal_in_battlefield_order_cr_510_1c` |
| CR 511.3 | At end of combat, all creatures and planeswalkers are removed from combat. | partial — creatures are removed (attacking/blocking cleared); planeswalkers/battles are not modeled. | `apply.rs :: remove_creatures_from_combat` | `apply.rs :: issue_117_end_of_combat_removes_creatures_from_combat_cr_511_3` |
| CR 601.2c | The targets chosen for a spell/ability are recorded on the stack object as it is put on the stack, and chosen from the freshly enumerated legal set as part of casting/activation. A targeted spell (e.g. a counterspell) chooses its targets exactly as an ability does, from the same effect-IR requirement machinery; a cast with no legal candidate for a slot is never offered. | partial — single-target-per-slot; "up to N", modal, and multi-target distinctness are not modeled. | `apply.rs :: apply_activate_ability`, `apply.rs :: apply_cast_spell`, `actions.rs :: action_target_specs`, `actions.rs :: valid_actions`, `ability.rs :: Target` | `actions.rs :: a_legal_target_is_accepted_and_carried_onto_the_stack`, `actions.rs :: issue_148_targeted_cast_advertised_once_with_the_spell_on_stack_as_a_candidate`, `actions.rs :: issue_148_targeted_cast_with_no_legal_candidate_is_not_offered`, `apply.rs :: issue_148_counterspell_counters_a_creature_spell_end_to_end_cr_701_5` |
| CR 605.1a | A mana ability is an activated ability whose every effect adds mana (and is not a targeted/loyalty ability). | partial — simplified: the "could add mana" / non-targeting nuances beyond "all effects add mana" are not modeled. | `ability.rs :: is_mana_ability` | `ability.rs :: activated_mana_ability_round_trips` |
| CR 605.3 | A mana ability resolves immediately without using the stack and does not change who has priority. | implemented | `apply.rs :: apply_activate_ability` | `apply.rs :: forest_mana_ability_adds_green_without_using_the_stack` |
| CR 608.2b | On resolution, an object's chosen targets are re-checked; if every target is now illegal the object is removed from the stack without effect ("fizzle"). A fizzled *spell* still goes to its owner's graveyard (a counterspell whose target already resolved fizzles); a fizzled ability ceases to exist. | implemented | `resolve.rs :: resolve_stack_object`, `resolve.rs :: target_is_legal` | `resolve.rs :: an_object_whose_target_became_illegal_fizzles`, `apply.rs :: issue_148_counterspell_fizzles_when_its_target_resolves_first_cr_608_2b` |
| CR 701.5 / 701.5a | Counter target spell: the targeted spell is removed from the stack and put into its owner's graveyard without resolving. A spell targets a `SpellOnStack` object chosen at cast; a mana ability never uses the stack (CR 605.3) and so can never be countered. | partial — "counter unless", counter-triggered effects, and countering abilities on the stack are not modeled. | `ability.rs :: Effect::CounterSpell`, `ability.rs :: TargetSpec::SpellOnStack`, `apply.rs :: apply_targeted_effect`, `resolve.rs :: target_is_legal` | `apply.rs :: issue_148_counterspell_counters_a_creature_spell_end_to_end_cr_701_5`, `actions.rs :: issue_148_counterspell_cannot_target_an_ability_on_the_stack_cr_605_3` |
| CR 701.7 | Destroy: the `Destroy` effect moves the targeted permanent to its owner's graveyard, the same graveyard path lethal damage uses; a destroy whose target has left first fizzles (CR 608.2b). | partial — regeneration and destruction-replacement effects are not modeled; owner is approximated by controller (no separate ownership yet). | `ability.rs :: Effect::Destroy`, `apply.rs :: apply_targeted_effect` | `apply.rs :: issue_149_destroy_puts_a_creature_in_its_owners_graveyard_cr_701_7`, `apply.rs :: issue_149_destroy_fizzles_if_its_target_left_first_cr_608_2b` |
| CR 608.2c | An individually illegal target is skipped on resolution while the object's still-legal targets take effect. | partial — no effect with more than one target slot exists yet, so only the single-target skip path is exercised. | `resolve.rs :: resolve_stack_object` | `resolve.rs :: an_object_whose_target_became_illegal_fizzles` |
| CR 608.2m | A resolving instant/sorcery is put into its owner's graveyard rather than creating a permanent. | partial — owner is approximated by controller (no separate ownership tracking yet). | `resolve.rs :: resolve_stack_object` | `resolve.rs :: issue_47_non_permanent_spell_resolves_to_graveyard_not_battlefield` |
| CR 608.3 | A resolving spell is routed by its card types: a permanent spell enters the battlefield, a non-permanent spell does not. | implemented | `resolve.rs :: resolve_stack_object`, `card.rs :: CardData::is_permanent` | `resolve.rs :: resolving_a_creature_spell_puts_it_on_the_battlefield` |
| CR 613.7 | Within a layer, continuous effects apply in timestamp order. | implemented (for the modeled layer) | `characteristics.rs :: ordered_pt_modifiers` | `characteristics.rs :: two_static_modifiers_apply_in_timestamp_order_and_sum` |
| CR 613.7c | Layer 7c: `+1/+1` / `-1/-1` counters, then static `+X/+Y` modifiers, adjust a permanent's power/toughness. | partial — only layer 7c is computed; layers 1–6 and set-P/T (CDAs) are deferred behind the same read path. | `characteristics.rs :: characteristics`, `characteristics.rs :: pt_counter_delta`, `characteristics.rs :: static_pt_delta` | `characteristics.rs :: single_static_modifier_stacks_on_printed_pt_and_counters` |
| CR 702.9 / 702.9c | Flying: a creature with flying can be blocked only by creatures with flying or reach. Enforced in block legality (per assignment, not by hiding candidates), so partial blocks of mixed flying/ground attackers stay expressible. | partial — flying's block restriction is enforced at declaration; keyword is printed card data (no keyword-granting effects yet). | `card.rs :: Keyword`, `combat.rs :: blocker_can_block_attacker`, `actions.rs :: blocks_selection_is_legal` | `combat.rs :: issue_153_flying_can_be_blocked_only_by_flying_or_reach_cr_702_9c`, `apply.rs :: issue_153_ground_creature_cannot_block_a_flyer_cr_702_9c` |
| CR 702.10 / 702.10b | Haste: a creature with haste ignores the summoning-sickness restriction on attacking (and on `{T}` abilities). | partial — the attack exemption is enforced (applied at the `attacker_candidates` call site over CR 302.6); the `{T}`-ability half of summoning sickness is itself unmodeled (see CR 302.6). | `card.rs :: Keyword`, `combat.rs :: attacker_candidates` | `combat.rs :: issue_153_haste_creature_is_an_attacker_candidate_cr_702_10b`, `apply.rs :: issue_153_hasty_creature_attacks_the_turn_it_enters_cr_702_10b` |
| CR 702.17 / 702.17b | Reach: a creature with reach can block creatures with flying. | partial — reach's blocking permission is enforced at declaration; keyword is printed card data. | `card.rs :: Keyword`, `combat.rs :: blocker_can_block_attacker` | `combat.rs :: issue_153_flying_can_be_blocked_only_by_flying_or_reach_cr_702_9c` |
| CR 702.20 / 702.20b | Vigilance: attacking doesn't cause a creature with vigilance to tap. | partial — the no-tap-on-attack effect is enforced; keyword is printed card data. | `card.rs :: Keyword`, `apply.rs :: apply_declare_attackers` | `apply.rs :: issue_153_vigilant_attacker_stays_untapped_and_can_block_next_turn_cr_702_20b` |
| CR 704 | State-based actions are checked and applied to a fixed point after every action. | partial — the SBA loop exists; CR 704.5a, CR 704.5c, and CR 704.5g are modeled (see below). | `sba.rs :: run_state_based_actions` | `sba.rs :: state_based_actions_reach_a_fixed_point` |
| CR 704.5a | A player with 0 or less life loses the game. The loss records its reason and consumes into the terminal result (CR 104.2a). | implemented | `sba.rs :: run_state_based_actions` | `sba.rs :: state_based_actions_mark_a_player_at_zero_life_as_lost`, `apply.rs :: issue_119_zero_life_loss_records_its_reason_cr_704_5a` |
| CR 704.5c | A player who attempted to draw a card from an empty library since the last SBA check loses the game (decking). The attempt is flagged by `Player::draw` at every draw site and consumed by the SBA loop. | implemented | `player.rs :: Player::draw`, `sba.rs :: run_state_based_actions`, `apply.rs :: draw_for_turn` | `sba.rs :: cr_704_5c_attempted_draw_from_empty_library_loses`, `apply.rs :: issue_119_decking_at_the_draw_step_loses_cr_704_5c` |
| CR 704.5g | A creature with toughness greater than 0 and marked damage greater than or equal to its toughness is destroyed (put into its owner's graveyard). | partial — destruction to graveyard is modeled; deathtouch as an alternate lethality source, regeneration, and separate ownership (owner is approximated by controller) are not. | `sba.rs :: run_state_based_actions`, `sba.rs :: has_lethal_damage` | `sba.rs :: cr_704_5g_creature_with_lethal_marked_damage_is_destroyed`, `sba.rs :: cr_704_5g_creature_below_lethal_survives`, `sba.rs :: cr_704_5g_lethality_reads_current_toughness_with_counters` |

## Keeping this current

When you add or change engine behavior that implements a CR rule:

1. Cite the rule as `CR NNN.Nx` in the doc comment of the code that implements
   it (the citation convention in [`coding-standards.md`](coding-standards.md)).
2. Add or update the corresponding row here — rule number, one-line summary,
   status, code anchor, and test anchor — in the **same PR**. Mark anything
   incomplete `partial` and name the gap.

This is part of the definition of done; see
[`agents/workflow.md`](agents/workflow.md).
