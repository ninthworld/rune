# Rust
/target
**/target
# Node
node_modules/
dist/
*.tsbuildinfo
# Playwright e2e (ADR 0011) run artifacts
playwright-report/
test-results/
.playwright/
# Environment / secrets
.env
.env.*
!.env.example
# GitHub App private keys (scripts/bot-token.sh). These live in ~/.config/rune,
# but never let a stray copy in the worktree become a commit.
*.pem
# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
# Logs
*.log
