# Rust
/target
**/target
# Node
node_modules/
dist/
*.tsbuildinfo
# Playwright e2e (ADR 0011) run artifacts
playwright-report/
test-results/
.playwright/
# Environment / secrets
.env
.env.*
!.env.example
# Private keys — never let a stray copy in the worktree become a commit.
*.pem
# Agent scratch — Claude Code creates isolated git worktrees here; never commit them.
.claude/worktrees/
# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
# Logs
*.log
